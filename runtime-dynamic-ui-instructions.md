# הוראות בנייה ותחזוקה — ראנטיים דינמי (Mayuvgam Runtime)

> **קובץ זה הוא מסמך הפעלה קבוע.** טען אותו בתחילת כל שיחה שעוסקת בראנטיים הדינמי, יחד עם
> שני קבצי הייחוס:
> 1. **האפיון הטכני** — קובץ ה-Excel בפורמט Mayuvgam (גיליונות: ישויות, שדות, זרימות עבודה,
>    הרשאות, טפסים, תצוגות, תבניות מייל, תפקידים, קבוצות, לוחות מחוונים, ווידג'טים).
> 2. **קובץ ה-HTML** — מסך ייחוס מעוצב (React) שמגדיר את המראה וההתנהגות הרצויים של הראנטיים.
>
> המסמך בנוי משני חלקים:
> - **חלק א' — יצירה חד-פעמית:** בנייה מחדש של הראנטיים כך שיֵראה ויתנהג כמו קובץ ה-HTML,
>   עם דריסת מבנה הנתונים הקיים וזריעה לפי האפיון הטכני. כולל חובת כפתור "חזרה לתצוגה קלאסית".
> - **חלק ב' — הוראות קבועות (רב-פעמי):** הכללים שמאפשרים, מעכשיו והלאה, לטעון אפיון טכני
>   (+ HTML אופציונלי) ולקבל מערכת עובדת — כולל תיקון השורש לבעיה ש"הזיהוי עובר אבל לא נוצר
>   כמעט כלום בבילדר".

---

## חלק 0 — מילון מושגים והקשר

| מושג | פירוש |
|------|-------|
| **Builder** | מסך התצורה. מקבל אפיון (Excel/JSON) ומייצר **bundle** — תיאור נתונים של המערכת (ישויות, שדות, טפסים, תצוגות, זרימות, הרשאות, דשבורדים). |
| **Bundle / config** | אובייקט הנתונים שה-Builder מפיק וה-Runtime צורך. פורמט: מפתחות בסגנון `"entities/<name>.yaml" → "<yaml string>"` (ראו `AI_INSTRUCTIONS.md` + `mayuvgam-examples.json`). |
| **Runtime** | מנוע הרינדור שמציג את המערכת למשתמשי הקצה לפי ה-bundle: ניווט, רשימות, טפסים, מסכי פירוט, דשבורדים, אשף הגשה, הרשאות לפי תפקיד. |
| **זיהוי (detection/ingest)** | השלב שבו האפיון מפורק וממופה ל-config מנורמל בזיכרון הראנטיים. |
| **תצוגה מעוצבת** | מצב התצוגה החדש שמחקה את קובץ ה-HTML (Linear/Stripe SaaS, RTL). |
| **תצוגה קלאסית** | מצב התצוגה הקיים/הגנרי של הראנטיים. **חייב להישאר זמין תמיד.** |

**עיקרון-על:** הראנטיים הוא *מונחה-נתונים* (data-driven). אסור לקודד "קשיח" את מערכת כרם.
כל מה שמופיע למטה לגבי כרם הוא **תוצאה של הזרעת ה-config מהאפיון הטכני** — אותו מנוע חייב לייצר
כל מערכת אחרת מאפיון אחר. כרם הוא רק ה-acceptance case של חלק א'.

---

# חלק א' — יצירה חד-פעמית: ראנטיים מעוצב + זריעה מהאפיון

מטרת החלק: לאחר ביצועו, פתיחת הראנטיים מציגה את **מערכת כרם — רישיונות יבואן** במראה זהה
לקובץ ה-HTML, כשהנתונים נובעים מה-config שנזרע מהאפיון הטכני, ועם כפתור מתמיד למעבר לתצוגה
הקלאסית.

## א.1 — שני מצבי תצוגה + כפתור "חזרה לתצוגה קלאסית" (חובה)

1. הוסף שכבת **View Mode** עם שני ערכים: `styled` (מעוצב) ו-`classic` (קלאסי).
2. שמור את הבחירה ב-`localStorage` תחת המפתח **`runtime-view-mode`**. ברירת מחדל: `styled`.
3. **הכפתור חייב להיות גלוי ונגיש בכל מסך** (מומלץ בפינת ה-topbar / ext-header):
   - בתצוגה מעוצבת: כפתור "🖥️ חזרה לתצוגה קלאסית".
   - בתצוגה קלאסית: כפתור "✨ תצוגה מעוצבת".
4. החלפת מצב **לא דורסת ולא מוחקת נתונים** — אותו config, שתי שכבות רינדור בלבד.
5. התצוגה הקלאסית נשארת בדיוק כפי שהיא היום (אל תשבור אותה). המעוצבת היא שכבה נוספת.
6. כל המסכים, הפעולות וההרשאות חייבים לעבוד זהה בשני המצבים — רק ה-styling/־layout משתנה.

> אם שכבת המעוצבת נכשלת ברינדור מסך כלשהו — **תבצע fallback אוטומטי לקלאסי** לאותו מסך
> ותרשום אזהרה לקונסול, במקום לקרוס.

## א.2 — דריסת מבנה הנתונים הקיים וזריעה מהאפיון

1. **אתחל מחדש** את ה-config/state של הראנטיים — מחק את מבנה הנתונים הקודם (אחרי גיבוי
   אוטומטי לזיכרון, ראו חלק ב' §ב.6).
2. בנֵה את ה-config מחדש מתוך **האפיון הטכני המצורף** לפי כללי הזיהוי בחלק ב' (§ב.2).
3. תוצאת הזריעה למערכת כרם (זהו ה-acceptance, לא קוד קשיח):

### ישויות (17)
`importer`, `request`, `request_product`, `request_product_classification`, `request_agent`,
`request_approval`, `document`, `request_log`, `request_product_log`, `document_status_log`,
`permit`, `permit_product`, `permit_agent`, `permit_product_classification`,
`permit_status_log`, `permit_product_status_log`, `user`.
לכל ישות: אייקון (lucide) מהאפיון, כותרת יחיד/רבים, וכל השדות מגיליון "שדות" (~100 שדות).

### תפקידים (5) וקבוצות (2)
- תפקידים: `representative` (מייצג), `import_department_manager` (מנהל תחום יבוא),
  `economist` (כלכלן), `garages_department` (תחום מוסכים),
  `head_of_maintenance_division_a` (מנהל אגף א' שירותי תחזוקה).
- קבוצות: `external_users` = {representative}, `internal_users` = {ארבעת הפנימיים}.
- מיפוי קטגוריה: קבוצת `external_users` → context **חיצוני** (פורטל); `internal_users` → **פנימי** (משרד).

### תצוגות (4 — מתוך גיליון "תצוגות")
- `request_list_external` — "הבקשות שלי" (חיצוני, ברירת מחדל).
- `pending_requests_internal` — "בקשות ממתינות לטיפול" (פנימי, ברירת מחדל).
- `permit_list` — "רישיונות יבואן".
- `importer_list` — "רשימת יבואנים".

### טפסים (2 — מתוך גיליון "טפסים")
- `request_submission_form` — "הגשת בקשה" → מרונדר כ**אשף רב-שלבי** (ראו א.4 ד').
  הסעיפים כוללים סעיפים מותנים לפי `request_type_cd` ו-`importer_type_cd`.
- `request_review_form` — "בחינת בקשה" → מרונדר כ**מסך פירוט עם טאבים** (ראו א.4 ג').

### דשבורד (1) + ווידג'טים (10)
`management_dashboard` עם הווידג'טים מגיליון "ווידג'טים": count / distribution / list,
כל אחד עם entity, color, group_by/filters/rows_limit כמפורט באפיון.

### זרימות עבודה ותבניות מייל
טען מגיליונות "זרימות עבודה" ו"תבניות מייל" (כולל `call_service`, `set_field`,
`show_error_message`, `after_save` וכו'). אם הראנטיים אינו מריץ אינטגרציות אמת — בצע
**הדמיה** (mock) של שירותים חיצוניים, אך שמור את ההגדרות ב-config כך שהן נראות וניתנות לעריכה.

## א.3 — מערכת העיצוב (Design System) — מתוך קובץ ה-HTML

החל את ערכי ה-`:root` והקומפוננטות של קובץ ה-HTML על שכבת התצוגה המעוצבת. עיקרי הטוקנים:

```css
/* צבעים */
--bg:#ffffff; --bg-subtle:#fafafa; --bg-muted:#f5f5f6;
--panel:#ffffff; --border:#ececef; --border-strong:#d8d8dc; --divider:#f0f0f2;
--text:#18181b; --text-secondary:#50525a; --text-muted:#7a7c85;
--accent:#5e6ad2; --accent-hover:#4d58c0; --accent-soft:#eef0fb; --accent-text:#3640a4;
/* סטטוסים סמנטיים (לכל אחד גם *-soft) */
--green:#1a7f4e; --amber:#b06b00; --red:#c1313b; --blue:#1e64bd;
--purple:#7548c8; --cyan:#0c7b89; --gray-text:#555560;
/* רדיוס / מרווחים / פריסה */
--r-sm:6px; --r-md:8px; --r-lg:12px; --r-xl:16px;
--sidebar-w:232px; --topbar-h:56px;
/* טיפוגרפיה: Heebo (גוף), JetBrains Mono (.mono); dir=rtl; font-size בסיס 14px */
```

**קומפוננטות ליבה לשכבה המעוצבת** (שמות ומחלקות מקובץ ה-HTML — השתמש בהן ככלים גנריים,
לא ספציפיים לכרם): `Button` (variants: primary/secondary/ghost/danger/success; sizes sm/lg/icon),
`Badge` + `StatusBadge` (badge-gray/blue/green/amber/red/purple/cyan/indigo + badge-dot),
`Card`, `Stat`, טבלה (`.tbl-wrap`/`.tbl-toolbar`/`.tbl-filter`/`.tbl-pager`),
`Field`/`Input`/`Select`/`Textarea`, `Modal`, `Stepper`, `Tabs`/`SubnavTabs`,
`Pager`, `Donut`, `BarRow`, `Toast` (+`ToastProvider`), `FileRow`, `Avatar`,
`choice`/`choice-grid` (כרטיסי בחירה), `chip`/`chip-group`, `timeline`, `kv-grid`, `action-bar`.

## א.4 — שלד האפליקציה והמסכים (מיפוי config → UI)

### א) שלד (App shell) — לפי `data-external`
- **פנימי (`internal_users`):** Grid עם **sidebar** (רוחב `--sidebar-w`): מותג "כרם / רישיונות
  יבואן", קבוצת "ניהול" עם פריטי ניווט מהתצוגות הפנימיות + מונים, קבוצת "תפעול"
  (תבניות מייל / לוגים / תפקידים והרשאות / הגדרות), פוטר משתמש. למעלה **topbar**: כותרת
  המסך, חיפוש, פעמון, עזרה, **RoleSwitcher**, וכפתור החלפת תצוגה.
- **חיצוני (`external_users`):** ללא sidebar (`.app[data-external="true"]`). **ext-header**
  עליון: מותג "כרם / פורטל יבואנים", ניווט אופקי ("הבקשות שלי" / "הרישיונות שלי"),
  **RoleSwitcher**, וכפתור החלפת תצוגה.
- **RoleSwitcher:** מאפשר החלפת תפקיד פעיל (להדגמה). החלפה לתפקיד חיצוני מנווטת ל"הבקשות
  שלי"; לפנימי — לדשבורד. הקשר (external/internal) נגזר מקבוצת התפקיד.
- שמור `roleId` ו-`page` ב-`localStorage` (כמו ב-HTML: `kerem-role`, `kerem-page`).
- **אכיפת הרשאות:** משתמש חיצוני מוגבל למסכים `my-requests`/`my-permits`/`new-request`
  ולפירוט בקשה. ניתוב/הצגת פעולות נגזרים מגיליון "הרשאות" (read/create/update/delete:
  none/own/team/all) — לא קשיח.

### ב) רשימות (views → list screens)
כל רשומה בגיליון "תצוגות" → מסך רשימה: טבלה עם העמודות, רוחבים, סוגי תצוגה
(`link`/`badge`/`text`/`number`/`date`/`currency`), מיון, ועימוד (`page_size`).
תמיכה ב-`type`: `table` (ברירת מחדל), `kanban` (לפי `kanban_field`+`kanban_columns`),
`calendar` (לפי `calendar_date_field`). שורת כלים עם פילטרים מהירים לפי שדות picklist
(למשל סטטוס בקשה). שורה לחיצה → מסך פירוט.

### ג) מסך פירוט (form type "review" → detail + טאבים)
`request_review_form` → מסך **בחינת בקשה** עם **SubnavTabs**, כאשר כל סעיף בטופס הופך לטאב:
"נתוני בקשה" (kv-grid/Fields), "תוצרים", "מסמכים" (FileRow + סטטוסים), "החלטה"
(action-bar עם אישור/דחייה/החזרה/הפקת רישיון לפי תפקיד), ובנוסף טאב "ציר זמן" שנגזר
מישויות ה-`*_log`. פעולות ההחלטה מפעילות את זרימות העבודה הרלוונטיות ומעדכנות סטטוסים.

### ד) אשף הגשה (form type "submission" → wizard)
`request_submission_form` → **SubmissionWizard** עם **Stepper**. כל סעיף = שלב.
**חובה לכבד את הסעיפים המותנים**: בטופס יש סעיפים נפרדים לפי `request_type_cd`
(רישיון חדש / הוספת תוצר / עדכון סיווגים / חידוש) ולפי `importer_type_cd`
(ישיר / עקיף — שמוסיף סוכנים). הצג רק את השלבים הרלוונטיים לבחירות המשתמש. שלב אחרון
"סקירה והגשה". בהגשה — הפעל את זרימת `submit_request` (קביעת סטטוס, רישום לוג).

### ה) דשבורד (dashboard → widgets grid)
`management_dashboard` → רשת **Stat**/כרטיסים + תרשימים:
- `count` → Stat עם המספר (מסונן לפי `filters`).
- `distribution` → Donut/BarRow מקובץ לפי `group_by`.
- `list` → טבלה קצרה (`rows_limit`) עם הפילטרים (כולל ביטויי `TODAY`/`ADD_DAYS`).
פתרון הפילטרים מחייב מיפוי **קוד↔תווית** ל-picklists (ראו §ב.2.4).

## א.5 — קריטריון קבלה לחלק א'

- [ ] פתיחת הראנטיים במצב מעוצב מציגה sidebar/topbar (פנימי) או ext-header (חיצוני) כמו ב-HTML.
- [ ] כפתור "חזרה לתצוגה קלאסית" גלוי בכל מסך, עובד, ונשמר ב-`localStorage`.
- [ ] מבנה הנתונים הישן נדרס; ה-config החדש מכיל 17 ישויות, ~100 שדות, 5 תפקידים, 4 תצוגות,
      2 טפסים, דשבורד עם 10 ווידג'טים — הכול מהאפיון, לא קשיח.
- [ ] החלפת תפקיד פנימי/חיצוני מחליפה ניווט והרשאות נכון.
- [ ] אשף ההגשה מציג שלבים מותנים נכון; מסך הבחינה מציג טאבים ופעולות החלטה לפי תפקיד.
- [ ] התצוגה הקלאסית עדיין עובדת ללא רגרסיה.

---

# חלק ב' — הוראות קבועות: אפיון טכני (+HTML) ⇒ מערכת עובדת

זהו החלק הרב-פעמי. הוא מגדיר את החוזה והכללים כך שכל אפיון טכני עתידי ייטען ויעבוד —
ובמיוחד **מתקן את הבאג ש"הזיהוי עובר אבל לא נוצר כמעט כלום בבילדר".**

## ב.1 — החוזה Builder ⇄ Runtime (מקור הבאג)

ה-bundle הוא אובייקט שמפתחותיו הם `"<section>/<name>.yaml"` והערכים הם מחרוזות YAML.
המקטעים: `entities`, `fields`(או משוקעים בתוך entity), `forms`, `views`, `workflows`,
`permissions`, `email_templates`, `groups`, `dashboards` (+widgets). ראו `AI_INSTRUCTIONS.md`
ו-`mayuvgam-examples.json` כמקור-אמת לפורמט, וגיליונות ה-Excel כמקור-אמת לתוכן.

> **למה "לא נוצר כלום": רשימת חשודים לבדיקה לפי סדר.**
> 1. **חוסר התאמת מפתחות/שורש** — ה-importer מצפה למבנה אחד (למשל `entity:` יחיד) וה-bundle
>    מספק אחר (מערך/מפתחות `entities/...`). זיהוי "עובר" (הקובץ נקרא/נפרס) אבל לולאת
>    היצירה לא מוצאת items → אפס כתיבות. **תקן: נרמל כל צורות הקלט למודל פנימי אחד.**
> 2. **YAML כמחרוזת לא מפוענח** — הערכים הם מחרוזות; חובה `YAML.parse` לכל ערך לפני שימוש.
> 3. **שדות בתוך entity מול גיליון fields נפרד** — יש לתמוך בשתי הצורות (שדות מקוננים בקובץ
>    הישות, או מערך fields עם `entity` כמפתח-זר).
> 4. **טפסים: שורה-לכל-סעיף** — מספר שורות עם אותו `form_name` הן **סעיפים בטופס אחד**, לא
>    טפסים נפרדים. importer נאיבי יוצר טפסים כפולים/ריקים ואז "מאחד" אותם לכלום.
> 5. **picklists עם קודים מספריים** — פילטרים/ווידג'טים מתייחסים לקודים (`status_cd==1`,
>    `IN (2,3,4,6,7)`) בעוד השדות מגדירים תוויות עבריות. בלי מיפוי קוד↔תווית — הפילטרים
>    מחזירים ריק והדשבורד נראה כאילו "לא נוצר".
> 6. **שדות relation** — `relation_entity`+`display_field`; אם לא נפתרים, הקשרים מושמטים.
> 7. **שדות מערכת** — `id`, `created_at`, `updated_at`, `created_by`, `updated_by`,
>    `owner_id`, `record_number` **לא** מופיעים באפיון; הראנטיים **מוסיף אותם אוטומטית**.
> 8. **כשל שקט בולע שגיאות** — `try/catch` שמדלג על מקטע פגום בלי לדווח. **אסור.** ראו §ב.5.

## ב.2 — שלב הזיהוי (ingest): כללי מיפוי קשיחים

מודל פנימי מנורמל אחד שכל הצורות ממופות אליו:
```
Schema = { entities:[{name,label,label_plural,icon,fields:[Field]}],
           forms:[{name,entity,label,is_default,sections:[{title,columns,fields:[{field,width}]}]}],
           views:[{name,entity,label,type,is_default,columns:[{field,display,width}],sort,page_size,
                   kanban_field,kanban_columns,calendar_date_field}],
           workflows:[...], permissions:[{role,entity,read,create,update,delete}],
           roles:[...], groups:[...], dashboards:[{name,...,widgets:[...]}],
           email_templates:[...], picklists:{<entity.field>:[{code,label}]} }
```

1. **קלט Excel:** קרא כל גיליון לפי `EXCEL_SHEETS_CONFIG` (ב-`modals/json-modal.js`).
   כל שורה = פריט. גיליון "טפסים" → קבץ לפי `form_name` ל-sections.
2. **קלט JSON bundle:** לכל מפתח `"<section>/<name>.yaml"` בצע `YAML.parse(value)` ומזג למודל.
3. **שדות מערכת:** הוסף אוטומטית `id, created_at, updated_at, created_by, updated_by, status?`
   לפי הצורך; אל תיצור עבורם שדות מהאפיון.
4. **מיפוי picklist קוד↔תווית:** לכל שדה picklist בנֵה אינדקס סדרתי **1-based**
   (`1→ערך ראשון, 2→שני, ...`) ושמור ב-`picklists["entity.field"]`. כל ביטוי פילטר/ווידג'ט
   שמשתמש בקוד מספרי ייפתר מול האינדקס הזה. תמוך גם בתווית ישירה.
5. **relations:** פתור `relation_entity`+`display_field`; אם היעד חסר — שמור placeholder
   ורשום אזהרה (אל תפיל).
6. **wizard vs detail:** טופס שכותרתו/שמו מרמז על הגשה (submission/הגשה) או שיש לו סעיפים
   מותנים → אשף; טופס בחינה/עריכה → מסך פירוט עם טאבים.
7. **סעיפים מותנים:** זהה תנאי מתוך כותרות הסעיפים (סוגריים: "(רישיון חדש - יבואן ישיר)")
   או ממטא-דאטה, ומפה ל-predicate על שדות הבקשה.
8. **ברירות מחדל חסרות:** אם לישות אין view ברירת-מחדל — צור אחת (table) מהשדות העיקריים.
   אם אין טופס — צור טופס חד-סעיפי מכל השדות שאינם מערכת.

## ב.3 — בנייה אוטומטית של מסכים מה-config

- כל **entity** → ניווט (אם יש לה view ברירת-מחדל), CRUD בסיסי, ומסך פירוט.
- כל **view** → מסך רשימה (§א.4ב).
- כל **form** → אשף או מסך-פירוט (§א.4 ג/ד).
- כל **dashboard** → רשת ווידג'טים (§א.4ה).
- **permissions** קובעות נראות פריטי ניווט, כפתורי פעולה, ושדות (read-only/נסתר).
- **workflows** רצות באירועים (on_create/on_update/on_field_change/before_save/after_save/
  on_form_load) ומבצעות set_field/validate/notify/call_service(mock)/show_error_message.

## ב.4 — טעינת HTML נלווה ⇒ הפקת עיצוב אוטומטית

כאשר מצורף קובץ HTML עיצובי (כמו מסך הייחוס):
1. חלץ את כל משתני ה-`:root` והחל אותם כ-design tokens של השכבה המעוצבת.
2. מפה מחלקות קומפוננטה ידועות (`.btn`, `.card`, `.badge-*`, `.tbl-*`, `.stepper`,
   `.subnav`, `.role-switcher`, `.app[data-external]` וכו') לקומפוננטות הגנריות של הראנטיים.
3. אמץ את שלד הניווט: sidebar לפנימי, ext-header לחיצוני, RoleSwitcher.
4. אם ה-HTML מגדיר color-map לסטטוסים — גזור אותו ל-`badge` color_map של ה-views.
5. **אל תייבא נתוני דמה (seed)** מתוך ה-HTML — הנתונים תמיד מהאפיון הטכני. ה-HTML הוא
   מקור לעיצוב/התנהגות בלבד.
6. אם אין HTML — השתמש בטוקנים מחלק א' כברירת מחדל לשכבה המעוצבת.

## ב.5 — עמידות ושקיפות (אסור כשל שקט)

1. **דיווח יצירה:** בסיום הזיהוי הצג סיכום: כמה ישויות/שדות/טפסים/תצוגות/זרימות/הרשאות/
   ווידג'טים נוצרו, וכמה דולגו ולמה. (זה מה שהיה חוסם את אבחון הבאג.)
2. **ולידציה מול הסכמה:** בדוק כל מקטע מול הפורמט הצפוי; פריט פגום → רשום שגיאה ספציפית,
   אל תבלע.
3. **לא אפס בשקט:** אם מקטע יצא ריק בעוד באפיון יש נתונים — זרוק/הצג שגיאה בולטת.
4. **fallback לקלאסי:** כשל ברינדור מעוצב של מסך → רנדר אותו מסך בקלאסי + אזהרה.
5. **אידמפוטנטיות:** טעינה חוזרת של אותו אפיון לא יוצרת כפילויות (מזג לפי `name`).

## ב.6 — שינויים "לפי דרישת המשתמשים"

1. **גיבוי לפני דריסה:** לפני אתחול config שמור snapshot (`runtime-config-backup`) לשחזור.
2. בקשת משתמש חופשית ("הוסף שדה X", "שנה צבע סטטוס", "פצל טופס") → תרגם לשינוי **ב-config**
   (לא ב-DOM), ואז רנדר מחדש. אל תקודד קשיח.
3. שמור על שמות מערכתיים (`name`) של ישויות/שדות קיימים — הם המפתח ליציבות הנתונים.
4. כל שינוי עיצובי → דרך design tokens/מחלקות, חל אוטומטית על שתי השכבות במידת האפשר.
5. שמור גרסאות config (history) כך שאפשר לחזור אחורה.

## ב.7 — Checklist לאבחון "לא נוצר כלום בבילדר"

1. האם ה-importer מבצע `YAML.parse` לכל ערך מחרוזת? (לא — תקן)
2. האם הוא תומך גם ב-fields מקוננים וגם בגיליון fields נפרד?
3. האם שורות טופס עם אותו `form_name` מקובצות לסעיפים (ולא לטפסים נפרדים)?
4. האם נבנה מיפוי קוד↔תווית ל-picklists, והפילטרים נפתרים מולו?
5. האם relations נפתרים מול `relation_entity`/`display_field`?
6. האם שדות מערכת מתווספים אוטומטית (ולא מצופים מהאפיון)?
7. האם יש דיווח יצירה מספרי, ושגיאות מקטע אינן נבלעות?
8. האם בכשל יש fallback לקלאסי במקום מסך ריק?

---

## נספח — קבצי ייחוס במאגר

- `AI_INSTRUCTIONS.md` — פורמט ה-bundle המלא (field types, forms, views, workflows, permissions, email).
- `mayuvgam-examples.json` — דוגמאות עובדות לכל סוג תצורה.
- `modals/json-modal.js` — `EXCEL_SHEETS_CONFIG` + `EXCEL_SECTION_GUIDE` (חוזה גיליונות ה-Excel).
- `schema/state-schema.json` — סכמת state להשוואה.
- קובץ ה-HTML המצורף — מקור-אמת לעיצוב ולהתנהגות התצוגה המעוצבת.
- קובץ ה-Excel המצורף — מקור-אמת לתוכן (מודל הנתונים של כרם).
